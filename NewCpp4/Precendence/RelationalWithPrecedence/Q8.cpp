/*
8. Negation with Relational Operators:
Problem: Evaluate the following expression:
h = !(8 >= 10);
Question: What is the value of h?
*/
#include<iostream>
using namespace std;
int main(){
    int h = !(8 >= 10);
    cout<<h;
}
//!(8 >= 10) is 8 greater or equal to 10 no so false now not op convert it into true